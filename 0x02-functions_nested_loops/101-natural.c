#include <stdio.h>

/**
 * main - Prints the sum 
 * Return: Always
 */

int main(void)

{
	int a, b = 0;

	while (i < 1024)
	{
	if ((a % 3 == 0) || (a % 5 == 0))
	{
	b += a;
	}
	a++;
	}
	printf("%d\n", z);
	return (0);

}
